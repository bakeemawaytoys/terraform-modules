---
# https://terraform-docs.io/user-guide/configuration/
formatter: markdown table

# Disable lock file parsing to ensure it doesn't read the files generated by TFLint when it initializes a module.
lockfile: false

output:
  file: README.md

settings:
  # Disable lock file parsing to ensure it doesn't read the files generated by TFLint when it initializes a module.
  lockfile: false

version: ~> 0.17
